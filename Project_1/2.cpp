#include"class.h"
//#include"functions.h"
int w1, w2, h1, h2;
int main()
{
    int screenWidth=GetSystemMetrics(SM_CXSCREEN);
    w=screenWidth;
    h=getmaxheight()+3;
    initwindow(w,h,"METRO APP", -2);
    setbkcolor(WHITE);
    cleardevice();
    first();
    //fares();
    closegraph();
}

